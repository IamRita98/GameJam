using System.Collections;
using System.Collections.Generic;
using System.Diagnostics;
using UnityEngine;
using UnityEngine.Events;

public class HousePuzzle : MonoBehaviour
{
/*
    public Transform[] pieces;
    // Start is called before the first frame update
    void Start()
    {
        //setting up board
        int x = 0;
        int y = 0;
        foreach (Transform child in PuzzleGridBase.transform)
        {
            PuzzlePIece piece = child.GetComponent<PuzzlePiece>();
            if (piece != null)
            {
                piece.onClicked.AddListener(OnPieceClicked);
            }
            if (y < 3)
            {
                pieces[x, y++] = child;
                continue;
            }
            x++;
        }
        x = 0;
        y = 0;
        Transform temp;
    }
    private void OnPieceClicked(GameObject piece)
    {

    }*/
}

